# vLLM + LangChain Demo 环境变量配置

# 代理配置（Docker 构建时使用）
# 注意：
# 1. 如果代理只监听 127.0.0.1，容器无法访问，需要：
#    - 让代理监听 0.0.0.0（推荐），或
#    - 使用 host.docker.internal（macOS Docker Desktop 支持），或
#    - 使用 Docker 网关 IP（如 172.17.0.1:7890）
# 2. macOS 用户可以使用 host.docker.internal
# 3. 如果代理监听 0.0.0.0，可以直接使用 localhost
BUILD_PROXY=http://localhost:7890
# 或者（如果 host.docker.internal 不可用）：
# BUILD_PROXY=http://172.17.0.1:7890

# vLLM 服务配置
VLLM_SERVER_URL=http://vllm-server:8001/v1
VLLM_MODEL_NAME=Qwen/Qwen2.5-1.5B-Instruct

