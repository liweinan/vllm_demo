# vLLM + LangChain Demo Environment Variables Configuration

# Proxy configuration (used during Docker build)
# Note:
# 1. If proxy only listens on 127.0.0.1, containers cannot access, need to:
#    - Let proxy listen on 0.0.0.0 (recommended), or
#    - Use host.docker.internal (macOS Docker Desktop supports), or
#    - Use Docker gateway IP (e.g., 172.17.0.1:7890)
# 2. macOS users can use host.docker.internal
# 3. If proxy listens on 0.0.0.0, can directly use localhost
BUILD_PROXY=http://localhost:7890
# Or (if host.docker.internal unavailable):
# BUILD_PROXY=http://172.17.0.1:7890

# vLLM Service Configuration
VLLM_SERVER_URL=http://vllm-server:8001/v1
VLLM_MODEL_NAME=Qwen/Qwen2.5-1.5B-Instruct

